# Feature Modules

Create feature folders under this directory:

- `knowledge-bases/`
- `documents/`
- `queries/`
- `settings/`

Each feature should own:

- page/container components
- hooks
- endpoint wrappers
- view model types
